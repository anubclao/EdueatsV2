/**
 * Deprecated worker entrypoint.
 * Queue worker support was removed; emails are handled directly in API flow.
 */

console.log('[EmailWorker] Worker deshabilitado. No hay proceso que iniciar.');
process.exit(0);
