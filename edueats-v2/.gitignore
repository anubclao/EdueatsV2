node_modules/
.env
.env.local
coverage/
# Ignorar TypeScript compilado del API
apps/api/dist/*.js
apps/api/dist/*.js.map
apps/api/dist/*.d.ts
apps/api/dist/routes/
apps/api/dist/db/
# Ignorar el dist de Vite (se copia a apps/api/dist/web/ con el build)
apps/web/dist/
# apps/api/dist/web/ se INCLUYE para que Hostinger tenga el frontend
# TypeScript incremental build info (artifact, regenerado por tsc)
*.tsbuildinfo
# Helpers locales de dev (no van al repo)
start-api-server.bat
# TypeScript incremental build cache (regenerated by tsc -b)
*.tsbuildinfo
# Helpers de debug local (no se commitean, quedan solo en este checkout)
start-api-server.bat
scripts/migrations/tools/

# Logs locales (debug de arranque)
*.log
server.out.log
server.err.log
